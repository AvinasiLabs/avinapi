1. All code comments MUST be written in English.
2. Always use `cargo add` command to get the latest versions of dependencies. Never manually modify `Cargo.toml`.
3. NEVER generate documentation files or scripts without explicit request.
4. NEVER generate future-use code or placeholder implementations. If found, delete immediately.
5. Before starting any development work, read all files in the `prompt/` directory to understand:
- Design patterns and architectural principles
- Implementation specifications and requirements
- Project structure and development guidelines
